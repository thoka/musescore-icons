# Werkzeuge fuer die Entwicklung (siehe tools/) und die Tests. Nicht noetig, um
# die Icons zu rendern -- dafuer genuegt requirements.txt.
playwright>=1.49
pytest>=8.0
pyyaml>=6.0  # docs/plans frontmatter + generated table (tools/pending.py)
